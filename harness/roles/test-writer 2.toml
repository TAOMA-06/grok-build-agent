# Test writer role — write and run tests
description = "Writes comprehensive tests following project conventions, then runs them"
default_capability_mode = "all"
reasoning_effort = "high"
default_fork_context = true
