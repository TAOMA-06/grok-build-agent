# Implementer role — edit + verify; not unrestricted "all"
description = "Implementer that edits code and addresses review notes"
default_capability_mode = "execute"
reasoning_effort = "high"
default_fork_context = true
