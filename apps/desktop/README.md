# Grok Build Desktop (app package)

Tauri + React frontend for the open-source Grok Build ACP client.

```bash
npm install
npm run tauri dev
```

See the [repository README](../../README.md) for full setup.
